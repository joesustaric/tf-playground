## create roles via IAMY? - no probs not keep it here.. sync to IAMY maybe..

# create cluster - ref VPC outputs for input state

# use latest ecs image from aws
# max 2 min 1 desired 1

# deploy a thing to it with a ALB .. slack bot? react app?